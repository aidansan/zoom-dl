(Need to have chrome installed)
1. Run the get_links.py file
2. Login to Zoom manually when the window opens
3. Need to keep window open while selenium clicks on links and moves on
4. Script will generate a link_texts.txt file
5. Run the download_links.py
6. Need to manually click enable multiple downloads
7. Need to keep window open until all files are downloaded